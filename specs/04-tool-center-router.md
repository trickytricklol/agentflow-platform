# 子规格 04：工具中台与智能路由

实现工具注册、JSON Schema、权限、手动/自动路由、语义匹配、渐进式两段加载、调用审计和成本统计。

验收：未触发工具不进入完整 Prompt；路由结果可解释；工具超时和异常可恢复。

当前实现：`ToolRouter.manifest()` 只返回轻量工具清单；`route()` 根据手动选择或查询语义挑选工具；只有被选中的 `ToolRoute` 才通过 `load_schemas()` 加载完整参数 Schema。
