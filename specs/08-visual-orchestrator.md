# 子规格 08：可视化编排客户端

实现画布拖拽、节点配置、连线校验、版本管理、运行调试、事件时间线、模型/工具/Agent 选择器和 Prompt 优化面板。

验收：浏览器可完成创建、校验、保存、发布和调试一次工作流；错误能定位到节点或边。

当前实现：`frontend/index.html` 提供零依赖画布 MVP，支持节点添加、拖动、连线、节点配置、基础校验和 DSL 导出。后续接入服务 API，并迁移到 React/React Flow。

当前前端已接入工作流保存、发布和 SSE 运行事件流，运行面板会逐条显示节点事件。
